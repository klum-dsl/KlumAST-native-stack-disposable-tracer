# KlumAST native-stack disposable tracer

This public repository is a short-lived, harmless tracer for KlumAST issue #716.
It contains no product code. Its three pull-request layers exercise GitHub's
native stacked-pull-request behavior and record only GitHub Actions event metadata.

Deletion condition: remove this repository, its branches, pull requests, workflow
runs, and any separately configured SonarCloud project after the approved tracer
receipt has been captured.
